/*首页面格式化时间 */
var time={
  formatToIndex:function(time){
    //时间操作  2018-11-18 14:28:08
    var t=time.split(' ');
    var t1=t[0].split('-');
    var t2=t[1].split(':');

    return t1[1]+'-'+t1[2]+'   '+t2[0]+':'+t2[1];
  }
}

module.exports={
  formatToIndex:time.formatToIndex
}